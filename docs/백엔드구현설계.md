# 방 중심 백엔드 구현 설계와 변경 기록

백엔드를 수정할 때 함께 갱신하는 구현 계약의 원본이다. 사용자 요구는 [기초 설계](기초설계.md), 실행 형태는 [로컬 서버 결정](결정/002-로컬서버-웹UI.md), 상태·검증 결과·구현 순서는 [프로젝트 문서](프로젝트.md#작업-목록)에서 관리한다. 이 문서에는 책임·호출 계약과 변경 이유를 남기고 작업 상태를 중복 관리하지 않는다.

## 구현 순서의 정정

먼저 연결 정의와 방 실행을 분리하고 가짜 의존성으로 검증한다. 그다음 실제 저장소를 연결한다. ‘저장 경계를 정한 뒤 곧바로 DB를 구현’하던 순서에 B01(연결·실행 계약과 구조), B02(실제 수신 경로 전환·연결 수명 관리)를 앞에 추가한다. 물리 저장 방식이 미정이어도 저장소 포트를 통해 구조를 검증할 수 있다. 메모리 어댑터는 이 단계의 검증용이며 제품의 영구 저장 요구를 충족하지 않는다.

## 객체와 책임

| 대상 | 책임 | 넣지 않는 내용 |
| --- | --- | --- |
| 모델 연결 정의 | 연결 ID·종류·주소·비밀 참조·활성 여부·revision | 키 원문, HTTP 클라이언트 |
| 메신저 연결 정의 | 연결 ID·플랫폼·비밀 참조·활성 여부·revision | SDK 객체, 방 컨텍스트 |
| 방 실행 설정 | 독립된 방 ID·이름·컨텍스트 사본·모델 선택·revision | 외부 채팅 ID를 방 ID로 사용하지 않음 |
| 모델 선택 | 연결 ID·모델 ID·지원 생성 옵션 | 연결 주소와 비밀값의 복사 |
| 입력 경로 | 연결·플랫폼·채널·인증된 사용자와 내부 방의 대응 | 연결 이름으로 권한 판단 |
| 실행 서비스 | 방 조회 → 적용 설정 고정 → 입력 접수 → 모델 호출 → 유효성 확인 → 결과 기록 | SDK 호출 형식·DB 구현 |
| 모델 실행 포트 | 연결 정의와 모델 선택을 실제 모델 호출로 변환 | 방 CRUD·외부 송신 |
| 저장소 포트 | 방 revision·입력 중복·결과 반영의 원자적 계약 | 특정 파일/DB 배치 |
| 조립 지점 | 저장소·비밀 공급자·실행 어댑터 주입과 종료 | 도메인 처리 규칙 |

기존 `ChatModel`과 LM Studio 어댑터를 재사용한다. 새로운 방 실행 서비스는 SDK에 의존하지 않는다. HTTP 클라이언트는 백엔드 수명 동안 공유하고 매 호출의 모델 설정은 방별로 구성한다. 연결 정의에 원문 키를 담지 않고 실행 직전에 비밀 공급자에서 조회한다. 주소·파라미터 검증은 기존 LM Studio 설정 검증을 재사용한다.

## 처리 계약

1. 생성한 방 ID는 외부 주소와 독립적이며 재사용하지 않는다. 방의 컨텍스트는 불변 사본으로 전달한다.
2. 모델 연결 ID는 존재하고 활성화되어 있어야 한다. 지원하지 않는 공급자로 자동 전환하지 않는다.
3. 수정에는 예상 revision이 필요하다. 실제 변경 시 revision을 증가시키고 오래된 편집을 거부한다.
4. 입력 식별자는 `(room_id, source, request_id)`다. source는 인증·라우팅 경계에서 정하며 외부 입력값을 권한 근거로 사용하지 않는다. 같은 키의 다른 본문은 충돌이다.
5. 한 방의 입력은 순서대로 처리하되 다른 방은 독립적으로 실행할 수 있다. 입력을 접수한 뒤 모델을 호출한다. 오류·취소를 접수 기록에서 구분하며 동일 키를 자동 재호출하지 않는다.
6. 호출 중 방 수정·삭제 또는 연결 수정·비활성화가 발생하면 늦은 결과를 대화에 반영하지 않는다. 메모리 검증 단계는 설정 revision 확인과 저장소의 조건부 완료를 사용한다. 영구 저장·다중 실행자 단계는 트랜잭션과 원자적 검증으로 강화한다.
7. 생성 결과 기록과 외부 송신은 분리한다. 새 실행 서비스는 생성 결과를 반환하며 실제 메신저 송신과 전송 결과 저장은 B02/I06에서 연결한다. 실패한 입력은 접수 기록에 남고 성공한 턴만 현재 모델의 대화 이력에 포함하는 초기 정책을 사용한다.
8. 방 삭제는 해당 방의 입력·이력도 제거한다. 늦은 호출로 방을 새로 만들지 않는다. 파일·DB 삭제 복구는 영구 저장 단계에서 추가한다.

## 기존 코드 전환 경계

| 구현 위치 | 역할 |
| --- | --- |
| [domain.py](../src/ai_companion/domain.py) | 연결 정의·모델 선택·방·입력·처리 상태·입력 경로 |
| [room_ports.py](../src/ai_companion/application/room_ports.py) | 비동기 저장·조회·모델 실행·비밀 공급·경로 조회 계약 |
| [rooms.py](../src/ai_companion/application/rooms.py) | 방 CRUD·방별 직렬 실행·중복·revision 검증과 라우팅 |
| [room_memory.py](../src/ai_companion/adapters/room_memory.py) | 구조 검증용 단일 프로세스 메모리 구현 |
| [model_executor.py](../src/ai_companion/adapters/model_executor.py) | LM Studio 설정 변환·호출 시 비밀 조회 |
| [bootstrap.py](../src/ai_companion/bootstrap.py) | `room_backend()`의 HTTP 클라이언트 생성·종료와 의존성 주입 |

기존 `run` CLI와 `ConversationService`는 Discord 연결 검증용 경로로 보존한다. 새 방 서비스·명시적 라우터·조립 함수를 먼저 작성하고 검증한 뒤 B02에서 기존 수신 경로를 전환한다. 인증되지 않은 첫 메시지로 방을 자동 생성하는 우회 구현을 도입하지 않는다.

모델 연결 정의·방 설정·실행 자원의 분리는 우선 구현하지만, 메신저 SDK 수신 작업의 공유·중지·재연결과 진행 중 전송은 별도 전환 단계에서 다룬다. 루프백 HTTP 서버·웹 API·스케줄러와 영구 저장은 아직 이 구조 검증 단계의 실행 경로에 포함하지 않는다.

검증용 메모리 구현은 단일 이벤트 루프·단일 `RoomService` 실행자를 전제로 한다. 대화 이력과 입력 상태를 프로세스 메모리에 보관하며 데이터 용량·모델 컨텍스트 예산·영구 중복 방지·다중 실행자 잠금은 제품용 저장/실행 단계에서 보완한다. 메신저 정의는 자료형만 제공하며 활성 연결 관리와 바인딩 generation은 B02 범위다. 조립 함수 호출자는 진행 중 요청을 종료한 후 컨텍스트를 닫아야 하며, 서버 종료 시 요청 취소·대기는 실제 서버 수명 관리에서 구현한다.

## 변경 기록

| 변경 | 이유와 영향 |
| --- | --- |
| 연결 정의·모델 선택·실행 자원 분리 | 공용 연결을 여러 방이 참조하면서 방별 모델·컨텍스트를 독립 적용하기 위함 |
| 독립된 방 ID와 revision 도입 | 외부 주소로 방을 식별하던 기존 구조를 대체하고 편집·삭제 중 늦은 결과 차단 |
| 비동기 저장소·모델 실행 포트 도입 | 물리 저장 선택과 무관하게 가짜 연결로 계약 검증, 이후 실제 저장소로 교체 |
| 입력 접수와 결과 기록 분리 | 송신 성공한 턴만 메모리에 추가하던 이전 계약에서 처리 상태와 중복 키를 분리 |
| 기존 CLI와 새 조립 경로의 단계적 전환 | 기존 회귀 검증을 유지하면서 새 방 구조를 먼저 검증 |

후속 수정은 이 표에 이유와 호환성 영향을 추가하고, 구현 사실과 검증 결과는 프로젝트 작업 원본에 갱신한다. DB 경계와 메신저 송신 정책이 바뀌면 [ERD](데이터설계.md)와 이 계약을 함께 조정한다.
